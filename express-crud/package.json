{
  "name": "express-crud-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install --include=dev && npm run build",
    "start": "cd server && npm start"
  }
}
