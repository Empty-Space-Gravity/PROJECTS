#include <stdio.h>
#include <conio.h>
void main()
{
    float length = 10.2;
    printf("The length is: %.2f\n",length*length);
    float breadth = 5.5;
    float area = 1/2.0 * length * breadth;

    printf("The area is %.2f\n", area);
}